<!DOCTYPE html>
<html>
<head>
	<title>UAuth - Login</title>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
	<meta name="viewport" content="width=device-width">
	<style>
body {
	margin: 0;
	padding: 0;
	background: #f5f5f5;
	font-family: Tahoma, "Helvetica Neue", Helvetica, "Segoe UI", Arial, freesans, sans-serif;
}

* {

	box-sizing: border-box;
}

#container {
	max-width: 300px;
	width: 100%;
	margin: 0 auto;
	padding: 50px 20px;
}

h1 {
	font-weight: normal;
	color: #047;
}

.center { text-align: center; }
#logo { width: 75%; }
#message { color: #e22; }

label {
	display: block;
	font-size: 12px;
	margin-bottom: 5px;
}

input, button {
	transition: background .3s ease-in-out;
	width: 100%;
	border: none;
	margin-bottom: 15px;
	height: 35px;
	padding: 0 10px;
}

input {	background: #ddd; font-size: 18px; }
input:hover, input:focus { background: #ccc; }

button {
	background: #047;
	color: #fff;
	cursor: pointer;
	font-size: 16px;
}

button:hover { background: #069; }
button:active { background: #07a; }

	</style>
</head>
<body>
	<div id="container">
		<div class="center">
			<a href="https://www.nottingham.edu.cn" target="_blank">
				<img id="logo" src="logo.png" alt="logo">
			</a>
			<h1>Log In</h1>
			<div id="message">{{message}}</div>
		</div>
		<form method="POST">
			<label for="username">Username</label>
			<input id="username" name="username" type="text" maxlength="50" value="{{username}}">
			<label for="password">Password</label>
			<input id="password" name="password" type="password" maxlength="50">
			<button type="submit">Log In</button>
			<input name="csrf" type="hidden" value="{{csrfToken}}">
		</form>
	</div>
</body>
</html>
